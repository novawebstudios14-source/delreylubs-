import {login} from '../actions';
export default async function Page({searchParams}:{searchParams:Promise<{erro?:string}>}){const {erro}=await searchParams;return <div className="public"><h1>Acesso da oficina</h1><p className="muted">Entre com a conta autorizada no Supabase.</p><div className="card"><form action={login}><label>Usuário ou e-mail<input name="identifier" required autoComplete="username"/></label><label>Senha<input name="password" type="password" required autoComplete="current-password"/></label>{erro&&<p role="alert">Usuário ou senha inválidos.</p>}<button>Entrar</button></form></div></div>}
